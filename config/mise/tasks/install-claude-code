#!/usr/bin/env bash
#MISE description="Claude Code のインストール（公式推奨のネイティブインストール）"

set -e

if command -v claude &> /dev/null; then
  echo "Claude Code already installed"
  exit 0
fi

echo "Installing Claude Code..."
curl -fsSL https://claude.ai/install.sh | bash
